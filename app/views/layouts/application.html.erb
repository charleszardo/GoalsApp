<!DOCTYPE html>
<html>
<head>
  <title>GoalsApp</title>
  <%= stylesheet_link_tag   'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>
  <nav>
    <% if logged_in? %>
    <span class="welcome">Welcome, <%= current_user.username %></span>
    <span class="logout-button"><%= button_to "Logout", session_url, method: :delete %></span>
    <div class="user-cheers-remaining">
      Cheers Remaining: <%= current_user.cheers_count %>
    </div>
    <% else %>
    <%= link_to "Sign Up", new_user_url %>
    <%= link_to "Sign In", new_session_url %>
    <% end %>
  </nav>
</header>
<section>
<%= yield %>
</section>
<footer>
  <% unless current_page?(leaderboard_goals_url)  %>
  <%= link_to "leaderboard", leaderboard_goals_url %>
  <% end %>
  <% unless current_page?(users_url)  %>
  <%= link_to "home", users_url %>
  <% end %>
</footer>
</body>
</html>
